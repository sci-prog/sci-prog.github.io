language: node_js

os:
  - linux

node_js:
  - "8"

install:
  - yarn

script:
  - yarn lint

after_success:
  - yarn build
  - ls

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: ./public
  target_branch: master
  on:
    branch: source
